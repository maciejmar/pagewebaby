import { ScrollToInShadowDirective } from './scroll-to-in-shadow.directive';

describe('ScrollToInShadowDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrollToInShadowDirective();
    expect(directive).toBeTruthy();
  });
});
