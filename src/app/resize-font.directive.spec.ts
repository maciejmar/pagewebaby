import { ResizeFontDirective } from './resize-font.directive';

describe('ResizeFontDirective', () => {
  it('should create an instance', () => {
    const directive = new ResizeFontDirective();
    expect(directive).toBeTruthy();
  });
});
